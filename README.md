# Object-_detection

Object detection via YOLOv3 with keras Training and Detecting Objects with YOLO3” by Huynh Ngoc Anh or experiencor

The project keras-yolo3 provide scripts to convert the pre-trained weights into Keras format, use the pre-trained model to make predictions, and provide the code required to distill interpret the predicted bounding boxes.
